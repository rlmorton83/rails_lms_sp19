<h1>New Course</h1>
<%= form_with(model: @course, local: true, class: 'ui form') do |f| %>
<div class="field">
    <%= f.labelk :name %>
    <%= f.text_field :name, required: true, placeholder: 'Course Name' %>
</div>
<%= f.submit nil, clkass: 'ui blue button inverted' %>

<%end%>